[package]
name = "yabane-extension-request-defaults"
version = "0.1.0"
edition = "2024"
description = "Provider and Endpoint request defaults extension for Yabane"
license = "MIT"

[dependencies]
bytes = "1"
http = "1"
serde_json = { version = "1", features = ["preserve_order"] }
yabane-extension-api = { path = "../../crates/yabane-extension-api" }
