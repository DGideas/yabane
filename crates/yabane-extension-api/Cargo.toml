[package]
name = "yabane-extension-api"
version = "0.1.0"
edition = "2024"
description = "Stable native extension API for Yabane"
license = "MIT"

[dependencies]
bytes = "1"
http = "1"
reqwest = { version = "0.12", default-features = false }
