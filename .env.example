# Copy this file to .env for local development.
# Never commit the real Turnstile secret.

# Cloudflare's documented always-pass test secret. When this value is used,
# Yabane automatically uses the matching test site key unless overridden below.
TURNSTILE_SECRET=1x0000000000000000000000000000000AA

# For production, set a matching widget pair and remove the test secret above.
# TURNSTILE_SITE_KEY=your-turnstile-site-key
# TURNSTILE_SECRET=your-turnstile-secret-key

# Comma-separated browser hostnames accepted during verification.
TURNSTILE_HOSTNAMES=localhost,127.0.0.1

# Optional upstream inference safety deadlines. Each value is in seconds and
# must be between 1 and 86400. Successful reads reset the read deadline, while
# the total deadline also bounds streams that send only keepalives.
# YABANE_UPSTREAM_CONNECT_TIMEOUT_SECONDS=15
# YABANE_UPSTREAM_READ_TIMEOUT_SECONDS=300
# YABANE_UPSTREAM_TOTAL_TIMEOUT_SECONDS=28800

# Optional logging setting. Use `yabane --addr 127.0.0.1:9090` to change
# the listener; the address is intentionally configured only on the command line.
# YABANE_LOG=info
